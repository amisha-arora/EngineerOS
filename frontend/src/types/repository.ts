export type RepositoryListItem = {
    id: string;
    name: string;
    url: string;
    createdAtUtc: string;
};
export type RepositoryDetails = {
    id: string;
    name: string;
    url: string;
    createdAtUtc: string;
};